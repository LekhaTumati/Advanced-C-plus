#include <iostream>

using namespace std;

int addnumbers(int x, int y, int z)
{
    int answer = x + y + z;
    return answer;
}

int main()
{
    cout << "the sum is " << addnumbers(5, 6, 7) << endl;
    return 0;
}
