#include <iostream>

using namespace std;

int main()
{
    int age = 178;
    if(age > 60)
    {
        if(age > 100)
        {
            cout << "Why are you still alive?\n";
        }
    }
    else
    {
        cout << "You are young, get yourself a job\n";
    }
    return 0;
}
