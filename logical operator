#include <iostream>

using namespace std;

int main()
{
    int age = 2;
    int money = 80000;

    if(age > 21 || money > 500)
    {
        cout << "you are allowed in!!\n";
    }
    if(age > 21 && money > 500)
    {
        cout << "you are allowed in!!\n";
    }
}
